server:
  port: 8006

spring:
  application:
    name: consul-provider-payment
  ####consul注册中心地址
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        # 服务名字
        service-name: ${spring.application.name}
        instance-id: ${spring.application.name}